/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // If you later use external image hosting, configure domains here.
    domains: [],
  },
}
module.exports = nextConfig;
